Interface:
    o change plot.NMFrankestimate to screeplot, remove class NMFrankestimate, 
    change it to normal NMFlist
    
Functions:

    o extractFeature should return the rownames if present.
    o incorporate pheatmap functions to produce nicer heatmaps
    o define a NMFsampler-class for MCMC based NMF
    o define NMFStrategyOctave
    o Store options as an environment in .GlobalEnv 

Documentation:
    o improve vignettes
    o migrate to roxygen2 generated man pages

Ideas:

    o Switch to the interface defined in the package modeltools
    o handle missing data in target matrix
